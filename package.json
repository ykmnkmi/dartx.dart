{
  "name": "dartx",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:wasm": "dart compile wasm -E --js-runtime-output=src/compile.js -o src/compile.wasm lib/compile.dart"
  },
  "devDependencies": {
    "monaco-editor": "^0.50.0",
    "vite": "^5.4.2"
  }
}